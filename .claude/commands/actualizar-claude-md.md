---
description: Actualiza CLAUDE.md (y el manual) con lo aprendido en esta sesión
---

Revisa la conversación de esta sesión e identifica todo lo que cambió o se aprendió sobre el
proyecto Asistente Wilson: decisiones tomadas, cambios en workflows, nuevos datos (números,
IDs, credenciales creadas — solo nombres, NUNCA valores secretos), nuevos gotchas/errores
resueltos, y pendientes nuevos o completados.

Luego:

1. Actualiza `CLAUDE.md` (raíz del repo) para que refleje el estado ACTUAL del proyecto:
   - Corrige datos obsoletos (workflows, autorizados, modelo, etc.)
   - Agrega gotchas nuevos a la sección correspondiente
   - Actualiza la lista de pendientes
   - Mantenlo CONCISO (es contexto de arranque, no un historial); lo extenso va al manual
2. Si hay cambios relevantes para el usuario, actualiza también
   `workflows-wilson/MANUAL-ASISTENTE-WILSON.md` (secciones afectadas y checklist).
3. NUNCA escribas secretos (tokens, API keys, contraseñas) en estos archivos.
4. Haz commit de los cambios con un mensaje claro en español y push a la rama
   `claude/daily-reminders-mobile-jsisf8`.
5. Termina con un resumen breve de qué actualizaste.
